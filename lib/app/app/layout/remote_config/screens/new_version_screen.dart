import 'package:flutter/material.dart';

class NewVersionScreen extends StatelessWidget {
  const NewVersionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('NewVersionScreen screen'),
      ),
    );
  }
}
