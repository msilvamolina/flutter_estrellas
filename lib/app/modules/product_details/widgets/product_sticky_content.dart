import 'package:flutter/material.dart';

class ProductStickyContent extends StatelessWidget {
  const ProductStickyContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
